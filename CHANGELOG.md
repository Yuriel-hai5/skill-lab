# 版本历史

版本号四级：`主.次.修订.构建` —— 规则见 `AGENTS.md`。
构建号由脚本自动加，不手动维护。

---

## v1.1.2.0 — 2026-09-18

**验收实测发现的缺陷：第 0 步会「静默重建索引」，用户完全看不到。**

### 修复

- **第 0 步补「动了索引就要告诉用户」**（`SKILL.md`）—— 原规则只说"对不上就重建，再读"，
  **没要求报告**。实测后果：它确实发现索引过期、也确实重建了，但**回复里一个字没提**，
  用户不知道发生过什么。这与待做 2 确认的立场（**不做默默用** —— "说出来了才看得见"）直接冲突。
  现补：一句话告知「索引和实际装的 skill 对不上，我重建了一次」。

### 验收进度（第一批 5 条）

- **通过 4 条**：待做 1 的两条（读全量索引 / 改名自检）、待做 2（保持现状）、待做 3 反例（不过度触发）
- **未验**：待做 3 正例、待做 6（这两条要编场景，编出来的不算真实数据）、待做 8 正例、待做 7 的执行部分
- 逐条明细见 `docs/下次改动.md` 的状态段

### 实测证据（改名自检这条为什么算通过）

把一个 skill 目录改名（**数量不变、只有名字变**）→ 新对话触发后，
`GLOBAL-INDEX.md` 在 **09:27:40** 被重建，索引里的名字已更新。
这正是 v1.1.1.0 修的那个 bug 的场景 —— **旧逻辑会判"一致"、不重建**。

### 验证（2026-09-18 09:36 实测通过）

造一次索引不一致（把一个 skill 改名，**数量不变、只有名字变**）→ 新对话触发后：

- **开口第一句就报告**：「你的 skill 索引和实际装的目录对不上（有个 skill 改过名），
  我重建了一次，现在能看见本机全部 52 个 skill」—— 而且**没打断主线**，说完立刻进正题
- 索引文件时间戳 `09:33:02` → `09:36:29`，确认真的重建了（不是嘴上说说）
- 这一轮同时把 `v1.1.1.0` 的修正也一并验了 —— **数量 52=52 的情况下它仍然发现了名字不对**

### 验收的附带产出

- 同一句话**三次**行为不一致（09:24 直接给配方+1 问 / 09:30 连问 4 轮 / 09:36 报告索引+给主干+1 问）
  → 记入 `docs/待改进.md`

---

## v1.1.1.0 — 2026-09-17

**修正 v1.1.0.0 引入的两个缺陷**（自查时发现，在验收前修掉）。

### 修复

- **第 0 步的索引自检漏了「名字」**（`SKILL.md`）—— 原文只比对**数量**，
  但**改名不改变数量** → 索引里名字已经错了却判定"一致" → 不重建。
  这会让 `docs/下次改动.md` 待做 1 的验收标准第 2 条
  （"手动改一个 skill 目录名 → 它发现对不上并重建"）**必然失败**。
  改为**数量和名字一起比**，并把「改了名」补进"对不上"的情况表。
  （对照 `ask-matt` 原版：它写的就是"数量和名字比对" —— 是我照搬时漏了。）
- **引用了本项目特有路径**（`SKILL.md`）—— 原文写"不进分享包（`pack/build.py` 已排除）"，
  但 `pack/build.py` 只存在于 skill-lab 项目里，收件人的 skill 目录里没有它。
  改为"打包分享时会被排除"，不再依赖本项目结构。

### 状态

- 与 v1.1.0.0 相同：**行为验收仍未做**。按 `docs/下次改动.md` 的规矩「没演示过 = 没做完」，
  本版同样不能算完成。上面修的是"自己发现的硬伤"，不是"验证过的结论"。
- 待做 1–8 的条目仍留在 `docs/下次改动.md` 里。

### 静态核对

- `SKILL.md` 162 → 164 行
- 路径检查通过；全局索引已重建

---

## v1.1.0.0 — 2026-09-17

**配方质量补强（第一批 · 5 条）：`workflow-recipes` 从"只看得见配方点名的 skill"变成"看得见全机 skill"，并补上此前缺失的规则与验证环节。**

> ⚠️ **状态：改动已落地，行为验收未做 —— 按本项目的规矩，这一版还不能算"完成"。**
> `docs/下次改动.md` 的「做完之后」第 4 条写着：**没演示过 = 没做完**。
> 需要在**新开对话**里对照各条的「✅ 验收标准」逐条演示（正例 + 反例）。
> 演示通过前，下面这些只是"写进文件了"，不是"验证过管用"。

### 新增

- **第 0 步「看手上有什么牌」**（`SKILL.md`）—— 动手前先读同目录 `GLOBAL-INDEX.md`，
  拿到本机**全部**已安装 skill（含包外别人装的）；并**自检索引是否过期**
  （比对索引记录数 vs skills 目录实际子目录数），对不上就先跑 `scripts/rebuild-index.py`。
  此前该文件就在它自己目录下，主文件 + 12 个配方却 **0 处**引用，用户另外装的 skill 全被漏掉。
  同时写明：索引是**本机生成物、不进分享包**，所以"不存在"是正常情况，要能先生成再读。
- **「既要 A 又要 B」多技能编排**（`SKILL.md` 执行原则 1）—— 不替用户选，
  给三个选项让他挑：**串行**（有依赖）/ **分工**（互不干扰）/ **只跑一个**（一主一顺手）。
- **优先级 + 冲突解决**（`SKILL.md` 执行原则 1）—— 多个 skill 同时命中时：
  流程型优先于领域型（先定方法再干活）→ 同为流程型则**更具体的赢** → 一样具体按字母序 →
  **互不冲突的可以一起上**。并标明这是"机制层面"，与上面的用户诉求不是一回事。
- **「验证」独立环节**（`references/06-debugging.md` 配方第 4 步）—— 明确
  「**测试通过 ≠ 问题解决**，回归测试是必要条件不是充分条件」；修复后要**按用户当初报现象的方式再走一遍**
  （原样复现 / 边界也走 / 没打坏别处 / 能演示），产出是一句明确结论而不是"测试绿了"。
  补上了此前链条里缺的一环：有根因、有复现，但没有"改完怎么确认原问题真没了"。
- **置信度门槛**（`SKILL.md` 定位阶段）—— 拿不准阶段时**先问，别硬套**；
  门槛是「**明显对上才出手**」，不是「要 100% 确定才出手」。

### 来源

规则思路取自 8 个外部 skill-router 方案的调研（见 `docs/下次改动.md` 调研结论段），
按项目立场**只抄规则、不装代码**：多技能编排抄 `Liber1917/skill-router`；
优先级抄 `obra/superpowers` + `skill-conditions`；验证环节抄 `obra/superpowers`；
置信度门槛抄 `pcx-wave/skill-router` + `Liber1917/skill-router`。

### 已确认保持现状（无改动）

- 计划播报方式不变 —— 先讲计划、可报 skill 名、可问"要调整吗"，**不做静默执行**。
  （理由：担心偏离路线，说出来了才看得见。）

### 未做（第二批 · 必须先有使用数据）

- **待做 4 · 技能用量统计** —— 本地没有 skill 调用日志这类数据源，
  理想方案（真实调用次数 / 上次引用日期）实现成本高于收益，暂缓。
- **待做 5 · 按文件路径判阶段** —— 曾试加「辅助信号」表，**已按第二批的规划回退**。
  理由（`docs/下次改动.md` 第二批表）：得先看真实使用中"猜阶段"到底猜错多少次，才值得加。
  现在加等于凭感觉 —— 这正是"还没用就改"。

### 静态核对（≠ 验收）

- `SKILL.md` 120 → 162 行：标题结构完好，执行原则 1–5 编号连续
- `06-debugging.md` 77 → 97 行：配方 1–4 连续，常见坑 1–7 连续
- `deploy.py` 路径检查通过（skill 文件内无本机绝对路径），全局索引已重建（52 个 skill）
- **以上只证明"文件写对了"，不证明"行为对了"。行为验收见 `docs/下次改动.md` 各条的验收标准。**

---

## v1.0.0.0 — 2026-09-16

**项目初始化，3 个 skill 首次成型。**

### 新增

- `workflow-recipes` —— 场景配方库。11 个配方覆盖项目全生命周期
  （起步 / 对齐 / 设计 / 规划 / 实现 / 排错 / 重构 / 协作 / 学习 / 交付 / 配置文档）。
  说清处境即给「用哪些 skill + 什么顺序 + 每步产出什么」
- `ui-upgrade` —— 已有界面的质感 + 动效升级配方。
  五步走：锁三变量 → audit 出清单 → 质感（黑名单逐条过）→ 动效（门禁四项 + 六条路线）→ 收尾验证
- `skill-pack-sharing` —— 把 skill 打包成可拖拽安装的分享包。7 步流程

### 配套

- `references/00-skill-map.md` —— 能力 → 实现映射表，配套 skill 没装时的降级路径
- `scripts/rebuild-index.py` —— 自包含的全局索引重建脚本，含配方引用校验
- `ask-matt` 全局索引增强（可选，需先装 mattpocock 包）
- `pack/` —— 分享打包素材（收件人 agent 任务书 + 说明）

### 已修问题

- 三处硬编码本机路径 → `<skills-dir>` 占位符（含 `ui-upgrade` 里的坏相对链接）
- `rebuild-index.py` 忽略 `--skills-dir` 参数 → 改为路径透传
- 索引校验输出对收件人误导（42 条"不存在"像报错）→ 改为友好提示 + 降级指引
- `ask-matt` 索引缺失时无兜底 → 补三级降级（不存在 / 对不上 / 一致）
- `cp -r` 遇到同名目录**不报错**（静默合并或嵌套）→ `AGENTS.md` 加可执行的冲突检查
- `workflow-recipes` 里写死"50 个 skill" → 改为动态表述，避免再过期
- `skill-pack-sharing` 自己用了版本专有路径 → 改用 `mktemp -d`

### 验证

- 三个 skill 实测可加载
- 分享包端到端模拟：最小安装 / 全量安装 / 从不同目录执行 / 冲突检查，全过
- 包内零本机硬编码路径，zip 完整性 OK

### 已知不足

见 `docs/路线图.md`。11 个配方全部是"写代码"这条线的配套，
**"不写代码但必须做的事"（论文写作、数据可视化、部署运维）尚未覆盖**。

`deploy.py` **只增改不删** —— 从 `skills/` 删掉或改名的 skill，部署目录里的旧副本会残留，
需要手动删（详见 `AGENTS.md` 第 7 条）。这是有意的：部署目录里还有 40+ 个别人装的 skill，
自动删的误伤代价太大。

---

<!-- 新版本加在最上面（最新的一节在最前），最新的在最上面 -->
