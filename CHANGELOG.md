# 版本历史

版本号四级：`主.次.修订.构建` —— 规则见 `AGENTS.md`。
构建号由脚本自动加，不手动维护。

---

## v1.5.0.0 — 2026-09-20

**正例过了，但暴露一条从没写下来的规则：`skill` 该默默用，别报给用户。**

### 怎么发现的

荆丝在项目里说了一句「**我看着别扭**」（**纯陈述**，没下指令、没提任何 skill 名），
那边的 AI 回：「**先按 skill 的铁律来：先量化，别猜**」。

| 判定 | 依据 |
|---|---|
| **正例 ✅ 过了** | 那句话有确切出处 —— `wpf-visual-bug-triage/SKILL.md:12`；那个 skill 的 `description` 里「**好别扭**」是**明写的触发词**；它开头就写着"用户只能给你一句主观感受……你的任务是把主观感受翻译成可量化的几何/状态错误"。**「只陈述别扭」是它预期的输入** |
| **但他不接受** | 原话：「确实正例过了，但是却**违背了之前的设想**，就是你可以**默默思考中使用，别说出来**」 |

**这是 skill-lab 从没写过的规则** —— grep 全仓库：**零条**说「别把 skill 名说出来」。
（`workflow-recipes/SKILL.md` 那条「**别默默做**」说的是**改本机文件**要报告，不是这个意思，别搞混。）

同族规则**只覆盖了一半**：`ui-upgrade/SKILL.md:46`「用用户能懂的话，**不要甩内部术语**」
（`eyebrow`/`scrim`/`hairline`）—— 那是**术语**层面；**skill 名**层面漏了。

### 🔬 元洞察：skill 文件的措辞会泄漏到对话里

它为什么说「**铁律**」？因为那个文件里那一行标题就写着 `## 铁律：先量化，别猜` ——
**它照搬了「铁律」这个词。**

> **skill 文件是给 agent 读的，但它的措辞会被复述给用户。**
> 写「铁律」，用户就会在对话里听到「按铁律」；写「先量化」，听到的就是「先量化」。
> **改文件措辞 = 改对话措辞。**

### 边界（他定的）

**分两种**：

| 场景 | 做法 |
|---|---|
| **单纯执行** | **默默用** —— 说「我接下来怎么做」，不说「我按哪个 skill 做」 |
| **需要他在两个做法之间选** | **必须说清是什么** —— 不说清他没法选 |

所以 v1.3.0.0 那条「有更贴的 skill → 一句话提出来让他选」**保留**，只是被明确成**唯一的例外**。

### 改动

| 文件 | 改了什么 |
|---|---|
| `AGENTS.md`（根） | 「先扫技能库」节下加 **`### 但用的时候，别报出来`**：❌/✅ 对照表 + 为什么 + 唯一例外 + 实测证据 + 措辞泄漏那条 |
| `workflow-recipes/SKILL.md` | 「派活时怎么做」那条加限定：**做法定了就默默用，别提 skill 名**；补 ❌/✅ 对照表；明确「唯一例外」就是上面那张表的第一行 |
| `references/11-setup-docs.md` | 配方 G 的**可粘贴 block** 里加一句「扫到了就默默用，别报 skill 名」（否则别人项目只学到"先扫"，学不到"别说"）；新增小节 **「扫到之后：默默用，别报出来」**（含措辞泄漏那条）；「常见坑」+第 7 条 |
| `~/.workbuddy-ai/skills/wpf-visual-bug-triage/SKILL.md` ⚠️ **仓库外** | 标题 `## 铁律：先量化，别猜` → **`## 先量化，别猜`**；标题下加「这条是对你的要求，不是对用户说的话……**说做法，不说出处**」 |

### 为什么 wpf 那个也改

它是**这次的实际触发点**。光加元规则不够 —— **措辞本身就在泄漏**，
把标题里的「铁律」去掉，模型就没有那个词可以照搬。

> `writing-for-agents` 也有相关的写作规范，但它**没有 `agent_created: true`** 标记，**改不了**。

### 副作用（已知，接受）

- 不提 skill 名 = 用户**看不到**"我查了技能库"这个动作。
  代价是**验收变难**（正例更难观察）—— 只能靠**问它**或**看行为**，不能靠看它嘴上说了什么
- 例外只有一处，边界窄 —— 但窄才可执行

### 验收标准

**正例**：在项目里说一句纯感受（「这里看着别扭」），**不提任何 skill 名** ——
看它是否**默默去查了**（行为上开始量化/先量图，而不是直接猜着改），
**且回复里不出现 skill 名、「铁律」这类词**。

**反例**：需要你**在两个做法之间选**时 —— 它**应该**说清是什么 skill。

> ⚠️ **状态：改动已落地，行为验收未做 —— 按本项目的规矩，这一版还不能算"完成"。**

---

## v1.4.1.0 — 2026-09-20

**把配方 G 用在自己身上：根 `AGENTS.md` 加「动手前先扫技能库」。**

### 改动

- 根 `AGENTS.md` 新增 **`## ⚠️ 动手前：先扫技能库（别跳）`**，放在**正文第一个 `##` 节**
  （越靠前越可能被认真对待）。内容：索引路径 + 4 行按本项目场景填的对照表 + 那行「为什么」的实例。
  末尾指向 `11-setup-docs.md` 的配方 G。
- 表格行按 **skill-lab 自己的场景**填（改 skill 写法 / 打包分享 / 用户描述处境 / 改界面），
  **不是**照抄模板 —— 按配方 G 的要求，抄错等于没写。

### 为什么现在加

- **它自己就是最好的试验田** —— 这个项目天天改 skill，是"skill 装了会不会被用上"的天然观测点
- 顺带成了配方 G 的**活样例**：以后要给别人项目加，直接指向这里

### 隐私注意

路径写成 `<skills-dir>` + `C:\Users\<你的用户名>\...`，**没有写本机用户名** ——
仓库是公开的，沿用 v1.2.0.0 的占位符规则。

### 验收标准

在这个项目里开新对话，随便派一件活（比如"改一下某个配方的措辞"），
看它**有没有先读 `GLOBAL-INDEX.md`**。

> ⚠️ **状态：改动已落地，行为验收未做。** 注意这条**不需要 deploy** ——
> `AGENTS.md` 在仓库根，不参与 `deploy.py`（只同步 `skills/`）。

---

## v1.4.0.0 — 2026-09-20

**v1.3.0.0 正例验收失败后，补上「强制它去查」的那一步 —— 机制层面的修复，不是文案调整。**

### 问题（v1.3.0.0 验收暴露）

`description` 已经改对了，验收结果仍是**失败**。三个硬事实：

| 事实 | 怎么知道的 |
|---|---|
| 新版 `description` **确实已加载** ✅ | AI 把 `workflow-recipes` 概括成「开发中报了个**现象不对**的问题」—— "现象不对"只在 v1.3.0.0 里出现过 |
| 触发**依然没发生** ❌ | 6 步准备全是读 `StripWindow.xaml`、grep `_barKind`，没查技能库 |
| 探针可信 | AI 自述「**没查。这是我的疏漏。**……不是『技能库没用』，是我压根没走那一步」—— 没顺着问话说"查了" |

**根因**：`description` 决定模型「**能看到**什么」，**不决定「会不会动手」**。
触发是模型的主动决策 —— 本 skill 自己就面临"不被触发"的问题，**所以它保不了自己**。

### 改动

| 文件 | 改了什么 |
|---|---|
| `references/11-setup-docs.md` | 新增 **配方 G · 让 skill 真的被用上 → 给项目加 `AGENTS.md`**：可粘贴的 block（每次对话自动注入，不依赖模型主动回忆）、表格行必须按项目自填、最后一行"为什么"要给实例不给口号、两个手段缺一不可（`description` 管看得见 / `AGENTS.md` 管会动手）；「适用信号」加一行；「常见坑」加第 6 条；「需确认」加一条（表格行让用户确认） |
| `SKILL.md` | 「派活时怎么做」的「为什么值得扫这一眼」后补一段：**别指望模型自己想起来**，指向配方 G；发现用户项目没这段就**主动提一次** |

### 为什么是"加一步"而不是"再改 `description`"

改 `description` 只在**边际上**提高概率，本质上还是"指望模型想起来"。
`AGENTS.md` 走的是另一条路：**每次对话自动注入** —— 不依赖模型主动回忆，可靠一个量级。

### 副作用（已知，接受）

- 这是 **per-repo** 的：每个项目要单独加一次，表格行要单独填
- `AGENTS.md` 每次对话都占 token（那一段约 200 字）—— 换来"skill 真的会被查"，划算

### 验收标准

在一个**没有**这段 `AGENTS.md` 的真实项目里干活，看本 skill 被触发后
**会不会主动提出"给这个项目加一段"**；加了之后再干活，看模型**是否真的先读索引**。

> ⚠️ **状态：改动已落地，行为验收未做 —— 按本项目的规矩，这一版还不能算"完成"。**

---

## v1.3.0.0 — 2026-09-19

**真实使用暴露的缺口：`workflow-recipes` 触发率太低，装了等于没被用上。**

### 问题（不是想出来的，是真用出来的）

在 Drawer 项目里开发了几次之后，荆丝问那个对话「最近几次开发用了什么 skill」——
回答是**一次都没调过**。查下来是两回事：

| 现象 | 判断 |
|---|---|
| `workflow-recipes` 没触发 | **正常** —— 他的原话是「改这里」「这里不对，我想的是 1…2…」，属于**派活**，不是"我不知道怎么办"。旧 `description` 只认后者 |
| `diagnosing-bugs` 适用却没触发 | **真问题** —— 但那个 skill 的触发词全是英文（`diagnose` / `broken` / `throwing` / `slow`），中文报 bug 撞不上。**它不是本仓库的 skill，改不了** |

**根因**：本 skill 的价值主张是「你说处境，我给配方」，
但真实开发中用户更多是「我知道要什么，你帮我做」——
**定位和实际用法之间有落差，触发率天然就低。**

### 改动

1. **`description` 补一类触发场景** —— 加上「在开发中报了一个『现象不对』的问题
   （bug、视觉缺陷、性能回退、说不清原因的不对劲）」。
   分界**刻意划在"现象类"而不是"任何指令"**：明确指令（"把 X 改成 Y"）**不触发** ——
   那时用户自己就知道要什么，套流程是打扰。
2. **正文新增「先分岔：他是『不知道怎么办』还是『知道要做什么』」一节**：
   - **求助**（"我卡住了"）→ 走原「三步走」，给多步序列
   - **派活**（"改这里"）→ **别套配方**，只做一件事：扫一遍 `GLOBAL-INDEX.md`，
     有更贴的专用 skill 就**一句话提出来让他选**；没有就**闭嘴干活**

### 为什么这条值得立刻改（而不是记进待改进）

- 它是**真用出来的** —— 之前那 8 条待做都还没经过真实使用验证，这条不是
- 它直击核心价值：**skill 不被触发，等于不存在**。本机装了 50+ 个 skill，
  触发率低意味着绝大多数永远不会被发现
- 荆丝原话：「不要什么都想待做，会越积越多的」

### 副作用（已知，接受）

放宽触发会让"现象类"对话多读一次本 skill（约 2K tokens）。
换来的是"可能发现更贴的专用 skill" —— 对装了 50+ skill 的机器，这笔账划算。

### 验收标准

在真实开发对话里报一个"现象不对"的问题（**不主动提任何 skill 名**），
看它有没有**扫索引**、并在**存在更贴 skill 时提一句**；
同时确认**下明确指令时不触发**（正例 + 反例都要走）。

> ⚠️ **状态：改动已落地，行为验收未做 —— 按本项目的规矩，这一版还不能算"完成"。**

---

## v1.2.0.0 — 2026-09-19

**方向 4 第一步：分享方式从「只能拖 zip」扩展到「给 GitHub 链接」。**

### 新增

- **支持从链接安装** —— 对方把仓库地址丢给自己的 AI agent，说一句「帮我装上」就能装，
  **不用先收文件**。

### 改动

| 文件 | 改了什么 |
|---|---|
| `pack/AGENTS.md` | **第 1 步从"解压"改成"拿到文件（两种入口）"** —— 入口 A 给链接（`git clone --depth 1`，挂死就换 codeload 下 zip）、入口 B 拖压缩包；新增「两种入口 skill 在哪不一样」对照表；第 2 步改用 `$SRC` 变量统一两种入口 |
| `pack/README.md` | 「懒人装法」从"拖压缩包"改成"给链接 / 给压缩包两种都行"；FAQ 同步 |
| `README.md`（根） | 顶部加**给外部 agent 的指路牌**（HTML 注释，人看不到）：说明"装 skill"到底指什么、指向 `pack/AGENTS.md`；「分享给别人」一节补「方式一 · 给 GitHub 链接」 |

### 为什么改这个

原设计假设**用户先收到文件**（zip 拖进对话）。但更常见的是**丢一个仓库链接让 agent 自己拉** ——
这种情况下 agent 从仓库根进来，看到的是"某个人的工坊"，**没有入口说明告诉它装什么、装到哪**。

### 公开前的隐私清理（同批）

- `.workbuddy-ai/`（3 个工作笔记）**从 git 全部历史中移除** —— `git filter-branch` 重写，
  全历史里出现次数**归零**；磁盘文件保留，只是不再跟仓库走
- 本机绝对路径统一换成 `C:\Users\<你的用户名>\...`（README / AGENTS / 下次改动）
- 速查表来源改指向仓库内副本 `docs/参考/`
- 仓库**仍为私有** —— 清理验证通过后再公开

### 注意

- **历史被重写**，提交 hash 全部改变（原 `486cdfa` 起的那串作废）
- `f582edb 记一笔：…` 那条被 `--prune-empty` 清掉（它只改了工作笔记，删完就变空了）
- **本次没跑 `deploy.py`** —— 改的是分享素材和文档，`skills/` 下的 skill 源码一行没动

### 验收标准（方向 4 原文）

> 找一个没用过的人试装，全程不问他问题，一次成功。

---

## v1.1.2.0 — 2026-09-18

**验收实测发现的缺陷：第 0 步会「静默重建索引」，用户完全看不到。**

### 修复

- **第 0 步补「动了索引就要告诉用户」**（`SKILL.md`）—— 原规则只说"对不上就重建，再读"，
  **没要求报告**。实测后果：它确实发现索引过期、也确实重建了，但**回复里一个字没提**，
  用户不知道发生过什么。这与待做 2 确认的立场（**不做默默用** —— "说出来了才看得见"）直接冲突。
  现补：一句话告知「索引和实际装的 skill 对不上，我重建了一次」。

### 验收进度（第一批 5 条）

- **通过 4 条**：待做 1 的两条（读全量索引 / 改名自检）、待做 2（保持现状）、待做 3 反例（不过度触发）
- **未验**：待做 3 正例、待做 6（这两条要编场景，编出来的不算真实数据）、待做 8 正例、待做 7 的执行部分
- 逐条明细见 `docs/下次改动.md` 的状态段

### 实测证据（改名自检这条为什么算通过）

把一个 skill 目录改名（**数量不变、只有名字变**）→ 新对话触发后，
`GLOBAL-INDEX.md` 在 **09:27:40** 被重建，索引里的名字已更新。
这正是 v1.1.1.0 修的那个 bug 的场景 —— **旧逻辑会判"一致"、不重建**。

### 验证（2026-09-18 09:36 实测通过）

造一次索引不一致（把一个 skill 改名，**数量不变、只有名字变**）→ 新对话触发后：

- **开口第一句就报告**：「你的 skill 索引和实际装的目录对不上（有个 skill 改过名），
  我重建了一次，现在能看见本机全部 52 个 skill」—— 而且**没打断主线**，说完立刻进正题
- 索引文件时间戳 `09:33:02` → `09:36:29`，确认真的重建了（不是嘴上说说）
- 这一轮同时把 `v1.1.1.0` 的修正也一并验了 —— **数量 52=52 的情况下它仍然发现了名字不对**

### 验收的附带产出

- 同一句话**三次**行为不一致（09:24 直接给配方+1 问 / 09:30 连问 4 轮 / 09:36 报告索引+给主干+1 问）
  → 记入 `docs/待改进.md`

---

## v1.1.1.0 — 2026-09-17

**修正 v1.1.0.0 引入的两个缺陷**（自查时发现，在验收前修掉）。

### 修复

- **第 0 步的索引自检漏了「名字」**（`SKILL.md`）—— 原文只比对**数量**，
  但**改名不改变数量** → 索引里名字已经错了却判定"一致" → 不重建。
  这会让 `docs/下次改动.md` 待做 1 的验收标准第 2 条
  （"手动改一个 skill 目录名 → 它发现对不上并重建"）**必然失败**。
  改为**数量和名字一起比**，并把「改了名」补进"对不上"的情况表。
  （对照 `ask-matt` 原版：它写的就是"数量和名字比对" —— 是我照搬时漏了。）
- **引用了本项目特有路径**（`SKILL.md`）—— 原文写"不进分享包（`pack/build.py` 已排除）"，
  但 `pack/build.py` 只存在于 skill-lab 项目里，收件人的 skill 目录里没有它。
  改为"打包分享时会被排除"，不再依赖本项目结构。

### 状态

- 与 v1.1.0.0 相同：**行为验收仍未做**。按 `docs/下次改动.md` 的规矩「没演示过 = 没做完」，
  本版同样不能算完成。上面修的是"自己发现的硬伤"，不是"验证过的结论"。
- 待做 1–8 的条目仍留在 `docs/下次改动.md` 里。

### 静态核对

- `SKILL.md` 162 → 164 行
- 路径检查通过；全局索引已重建

---

## v1.1.0.0 — 2026-09-17

**配方质量补强（第一批 · 5 条）：`workflow-recipes` 从"只看得见配方点名的 skill"变成"看得见全机 skill"，并补上此前缺失的规则与验证环节。**

> ⚠️ **状态：改动已落地，行为验收未做 —— 按本项目的规矩，这一版还不能算"完成"。**
> `docs/下次改动.md` 的「做完之后」第 4 条写着：**没演示过 = 没做完**。
> 需要在**新开对话**里对照各条的「✅ 验收标准」逐条演示（正例 + 反例）。
> 演示通过前，下面这些只是"写进文件了"，不是"验证过管用"。

### 新增

- **第 0 步「看手上有什么牌」**（`SKILL.md`）—— 动手前先读同目录 `GLOBAL-INDEX.md`，
  拿到本机**全部**已安装 skill（含包外别人装的）；并**自检索引是否过期**
  （比对索引记录数 vs skills 目录实际子目录数），对不上就先跑 `scripts/rebuild-index.py`。
  此前该文件就在它自己目录下，主文件 + 12 个配方却 **0 处**引用，用户另外装的 skill 全被漏掉。
  同时写明：索引是**本机生成物、不进分享包**，所以"不存在"是正常情况，要能先生成再读。
- **「既要 A 又要 B」多技能编排**（`SKILL.md` 执行原则 1）—— 不替用户选，
  给三个选项让他挑：**串行**（有依赖）/ **分工**（互不干扰）/ **只跑一个**（一主一顺手）。
- **优先级 + 冲突解决**（`SKILL.md` 执行原则 1）—— 多个 skill 同时命中时：
  流程型优先于领域型（先定方法再干活）→ 同为流程型则**更具体的赢** → 一样具体按字母序 →
  **互不冲突的可以一起上**。并标明这是"机制层面"，与上面的用户诉求不是一回事。
- **「验证」独立环节**（`references/06-debugging.md` 配方第 4 步）—— 明确
  「**测试通过 ≠ 问题解决**，回归测试是必要条件不是充分条件」；修复后要**按用户当初报现象的方式再走一遍**
  （原样复现 / 边界也走 / 没打坏别处 / 能演示），产出是一句明确结论而不是"测试绿了"。
  补上了此前链条里缺的一环：有根因、有复现，但没有"改完怎么确认原问题真没了"。
- **置信度门槛**（`SKILL.md` 定位阶段）—— 拿不准阶段时**先问，别硬套**；
  门槛是「**明显对上才出手**」，不是「要 100% 确定才出手」。

### 来源

规则思路取自 8 个外部 skill-router 方案的调研（见 `docs/下次改动.md` 调研结论段），
按项目立场**只抄规则、不装代码**：多技能编排抄 `Liber1917/skill-router`；
优先级抄 `obra/superpowers` + `skill-conditions`；验证环节抄 `obra/superpowers`；
置信度门槛抄 `pcx-wave/skill-router` + `Liber1917/skill-router`。

### 已确认保持现状（无改动）

- 计划播报方式不变 —— 先讲计划、可报 skill 名、可问"要调整吗"，**不做静默执行**。
  （理由：担心偏离路线，说出来了才看得见。）

### 未做（第二批 · 必须先有使用数据）

- **待做 4 · 技能用量统计** —— 本地没有 skill 调用日志这类数据源，
  理想方案（真实调用次数 / 上次引用日期）实现成本高于收益，暂缓。
- **待做 5 · 按文件路径判阶段** —— 曾试加「辅助信号」表，**已按第二批的规划回退**。
  理由（`docs/下次改动.md` 第二批表）：得先看真实使用中"猜阶段"到底猜错多少次，才值得加。
  现在加等于凭感觉 —— 这正是"还没用就改"。

### 静态核对（≠ 验收）

- `SKILL.md` 120 → 162 行：标题结构完好，执行原则 1–5 编号连续
- `06-debugging.md` 77 → 97 行：配方 1–4 连续，常见坑 1–7 连续
- `deploy.py` 路径检查通过（skill 文件内无本机绝对路径），全局索引已重建（52 个 skill）
- **以上只证明"文件写对了"，不证明"行为对了"。行为验收见 `docs/下次改动.md` 各条的验收标准。**

---

## v1.0.0.0 — 2026-09-16

**项目初始化，3 个 skill 首次成型。**

### 新增

- `workflow-recipes` —— 场景配方库。11 个配方覆盖项目全生命周期
  （起步 / 对齐 / 设计 / 规划 / 实现 / 排错 / 重构 / 协作 / 学习 / 交付 / 配置文档）。
  说清处境即给「用哪些 skill + 什么顺序 + 每步产出什么」
- `ui-upgrade` —— 已有界面的质感 + 动效升级配方。
  五步走：锁三变量 → audit 出清单 → 质感（黑名单逐条过）→ 动效（门禁四项 + 六条路线）→ 收尾验证
- `skill-pack-sharing` —— 把 skill 打包成可拖拽安装的分享包。7 步流程

### 配套

- `references/00-skill-map.md` —— 能力 → 实现映射表，配套 skill 没装时的降级路径
- `scripts/rebuild-index.py` —— 自包含的全局索引重建脚本，含配方引用校验
- `ask-matt` 全局索引增强（可选，需先装 mattpocock 包）
- `pack/` —— 分享打包素材（收件人 agent 任务书 + 说明）

### 已修问题

- 三处硬编码本机路径 → `<skills-dir>` 占位符（含 `ui-upgrade` 里的坏相对链接）
- `rebuild-index.py` 忽略 `--skills-dir` 参数 → 改为路径透传
- 索引校验输出对收件人误导（42 条"不存在"像报错）→ 改为友好提示 + 降级指引
- `ask-matt` 索引缺失时无兜底 → 补三级降级（不存在 / 对不上 / 一致）
- `cp -r` 遇到同名目录**不报错**（静默合并或嵌套）→ `AGENTS.md` 加可执行的冲突检查
- `workflow-recipes` 里写死"50 个 skill" → 改为动态表述，避免再过期
- `skill-pack-sharing` 自己用了版本专有路径 → 改用 `mktemp -d`

### 验证

- 三个 skill 实测可加载
- 分享包端到端模拟：最小安装 / 全量安装 / 从不同目录执行 / 冲突检查，全过
- 包内零本机硬编码路径，zip 完整性 OK

### 已知不足

见 `docs/路线图.md`。11 个配方全部是"写代码"这条线的配套，
**"不写代码但必须做的事"（论文写作、数据可视化、部署运维）尚未覆盖**。

`deploy.py` **只增改不删** —— 从 `skills/` 删掉或改名的 skill，部署目录里的旧副本会残留，
需要手动删（详见 `AGENTS.md` 第 7 条）。这是有意的：部署目录里还有 40+ 个别人装的 skill，
自动删的误伤代价太大。

---

<!-- 新版本加在最上面（最新的一节在最前），最新的在最上面 -->
